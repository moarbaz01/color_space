import Navbar from "@components/Navbar";
import { Container } from "@mui/material";

const NotFound = () => {
  return (
    <Container>
      <Navbar />
    </Container>
  );
};

export default NotFound;
